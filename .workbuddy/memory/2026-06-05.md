# 2026-06-05 工作记录

### 17:58 联系方式区域更新：QR码 + 微信公众号
- 复制 `微信二维码.png` → `wechat-qr.png`，`小红书二维码.png` → `xhs-qr.png` 到项目目录
- 替换 GitHub 联系方式为 **微信公众号**：新 SVG 图标（文档+通知角标）+ 二维码占位（"敬请期待"）
- **小红书**：改为书本图标 SVG + 二维码图片，hover 时 logo 上浮 + 二维码渐显
- **微信**：保留聊天气泡图标 + 二维码图片，hover 交互同上
- 新增 CSS：`.has-qr`、`.contact-qr`、`.qr-placeholder`、`.contact-label`
- 响应式：480px 下二维码缩至 100x100px
- 交互方式：鼠标移到 logo → logo 上浮 8px + 二维码/文字渐显

### 18:15 联系方式统一排版
- 所有4项统一结构：`logo → label → detail`，上中下垂直排列
- 全部改为 `<div>` 标签，移除所有 `<a>` 链接和跳转交互
- 文本统一精简：微信、小红书、微信公众号、邮箱（去掉 @Yan、@YanStudio）
- 新增 `.contact-email` 样式（等宽字体、淡化色）
- `.contact-item`：`cursor: default` + `user-select: none`，行为一致

### 21:00 模块间距全面缩减
- `section` padding：120→90px | `section-heading` mb：56→36px | `section-label` mb：20→14px
- `#works` padding：140→100px | `.works-header` mb：64→40px
- `#articles` padding：140→100px | `.articles-grid` mb：48→32px
- `#contact` padding：140/180→100/130px | contact heading 内联 mb：80→50px
- `.hero-grid` gap：80→60px | `.contact-grid` gap：40→32px
- 同步更新 4 个响应式断点（1024/768/480/1440+）的 section padding

### 21:25 页面边界优化 + 作品轮播重设计
**左右边界收紧：**
- nav/section/#works/#articles/#contact/.carousel-info/.site-footer 水平 padding：60px → 50px
- `.section-inner` max-width：1200 → 1280px
- 4 个响应式断点水平 padding 同步缩减（36→32, 24→20, 16→14, 120→100）

**作品轮播布局重构：**
- 移除全部 6 个 `.side-thumb` 缩略图（HTML + CSS）
- `.center-main`：660px 固定 → `100% / max-width:880px`，全宽居中
- `.carousel-viewport` max-width：1100 → 1000px
- 左右箭头定位从 `calc(50% - 370px)` 改为 `left:16px / right:16px`
- 响应式断点同步清理 side-thumb 规则，center-main 改为 max-width 约束
- Group 1（GIF）补回播放按钮居中覆盖

### 21:35 移除播放按钮 + 大图比例改 21:9
- 移除 3 个 carousel group 的全部 `.play-btn` HTML 及 CSS 规则（含响应式断点清理）
- `.center-main .slide-media` aspect-ratio：16/9 → 21/9（超宽银幕比例）

### 21:40 还原 16:9 + 板块间距再次缩减
- aspect-ratio 从 21:9 还原为 16:9（21:9 太扁违和）
- 通用 section padding：90 → 60px | #works/#articles：100 → 80px | #contact：100/130 → 80/100px
- 响应式：1024（80→50）、768（64→40）、480（48→32）、1440+（140→110）全面缩减
