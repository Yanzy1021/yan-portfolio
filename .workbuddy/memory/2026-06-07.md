# 2026-06-07 工作日志

## v2.3.4 调整
1. all.html 文章列表恢复时间轴布局（替代横向卡片）
2. 主页文章卡片去掉描述，仅保留图片+日期+标题
3. 常用工具展示数量从 6 个提升到 10 个
4. 非 hero section 上下 margin 从 20px 调整到 50px
5. 修复 all.html renderToolCard 遗留的死代码

## v2.4.0 数字分身管理（第一层）
1. 新增 data/avatar.json 存储数字分身配置
2. server.js 新增 GET/PUT /api/avatar 接口
3. admin.html 新增「数字分身」管理面板（分身名称/头像/欢迎语/状态文案 + 预设话术增删 + 快捷回复按钮）
4. index.html 数字分身改为从 /api/avatar 动态加载配置，替代硬编码
5. 新增快捷回复按钮功能（聊天输入框上方标签按钮）
6. 修复 admin loadAll() 漏掉 loadTools() 和 loadAvatar() 的问题
7. 聊天面板右上角新增关闭按钮（.chat-close-btn）

## v2.5.0 Vercel 部署适配
1. 新增 .gitignore（排除 data/admin.json、uploads/、.env 等）
2. 新增 vercel.json 部署配置（路由重写 + 缓存策略）
3. 新增 lib/kv.js — 数据读写抽象层（Vercel KV 优先，本地 JSON 兜底）
4. 新增 lib/auth.js — 认证中间件（Vercel KV token 存储）
5. 新增 lib/upload.js — 上传工具（Vercel Blob 优先，本地 fs 兜底）
6. 新增 api/ 目录：13 个 serverless 函数替代 server.js 的 Express 路由
7. 新增 seed.js — 数据初始化脚本（本地 JSON 导入 Vercel KV）
8. package.json 新增 @vercel/kv、@vercel/blob 依赖
9. footer 下间距改为 10px，.site-footer 上间距改为 0px
