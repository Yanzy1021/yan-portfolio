# 2026-06-07 工作记录

## 个人主页 v1.0.0 版本管理 + v2.0.0 管理后台开发

### v1.0.0 版本管理
- 在 index.html 页脚添加版本号 v1.0.0
- 初始化 Git 仓库，提交并打标签 v1.0.0
- 备份 index.html 到 backups/v1.0.0-index.html

### v2.0.0 管理后台
- 搭建 Node.js 后端 (server.js)，基于 Express + bcryptjs + multer
- 创建管理后台页面 (admin.html)，极简黑白风格
- 主站改为 API 动态加载作品/文章/联系方式（离线模式保留默认数据）
- 账号密码登录：默认 admin/admin123（首次登录后需修改）
- 隐藏管理入口：点击 logo 5次 / URL 参数 ?admin=1
- 数据存储：data/ 文件夹（JSON 文件）
- 一键启动脚本：start.bat（Windows 双击即用）
- Git 打标签 v2.0.0

### 用户信息确认
- 用户技术背景：完全不懂，需要全包服务
- 用户偏好：方案B（轻量后端 Node.js + JSON 文件）

### 管理后台文件上传改造
- 作品封面从"图片URL"输入框改为拖拽上传区（支持图片 + 视频）
- 联系方式二维码从路径输入改为上传按钮 + 预览
- 上传文件存入 uploads/ 目录，服务通过 /uploads/ 路径提供静态文件
- 修复了 works.json 中绝对路径问题，将已有图片迁移到 uploads/
- 作品列表卡片支持视频缩略图悬浮播放

### 修复文章/笔记创建 + 图片上传 401 问题
- 修复文章/笔记创建失败：server.js 的 POST /api/articles 缺少 image 字段
- 重写文章弹窗，添加拖拽上传封面图功能，字段与作品一致（标题、描述、封面、链接、发布日期）
- 修复图片上传 401 错误：服务器重启后内存 token 清空，但客户端 localStorage 仍保留旧 token
  - 新增 GET /api/check-auth 端点验证 token 有效性
  - 页面加载时先验证 token 是否有效，无效则清除并显示登录页
  - api() 和 uploadFile() 捕获 401 自动跳转登录页并提示"登录已过期"
